// import something here
import { AddressbarColor } from 'quasar'
// "async" is optional
export default async ({ /* app, router, Vue, ... */ }) => {
  // something to do
  AddressbarColor.set('#fea500')
}
